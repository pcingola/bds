#!/usr/bin/env bds


try {
	# This task should fail (command not found)
	task fake_command_does_not_exits "Hello"

	# Wait within try/catch, so we capture the exception
	wait
} catch (Exception e) {
	println "Catch!"
}

exit 0
