#!/usr/bin/env bds

z := 42
z_list := ['a', 'b']
int xx

int myFunc(int x, int y) {
    i := 3
    return x + y + z
}

int myFunc2(int x) {
    return x + 1
}

class MyClass {
    int j

    int sum(int x) { 
        return j + x
    }
    int mul(int x) { 
        return j * x
    }
}

println("Hello World")
myFunc(3, 5)
myFunc2(42)

