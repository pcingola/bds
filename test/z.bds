#!/usr/bin/env bds

in := "input.txt"	help Input file name
out	:= "out.txt"	help Output file name

# Trivial example task
task(out <- in, cpus := 1) {
	sys echo "processing file '$in"
	sys cat $in \
		| grep "DATA" \
		> $out
		
}

