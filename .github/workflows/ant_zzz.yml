name: BDS Build CI

on: [push]

jobs:
  build-zzz:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v2
    - name: Run distro.sh
      run: set
    - name: Run distro.sh
      run: |
        cd $GITHUB_WORKSPACE
        echo "DIR=$(pwd -P)"
        ls -al
        ./scripts/distro.sh
      shell: bash
